<network-security-config>
    <!-- Debug builds only: the emulator's 10.0.2.2 alias reaches the host's
         plain-http dev server (see app/build.gradle.kts API_BASE_URL). A
         release build talks to a real https:// domain and never needs this. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
